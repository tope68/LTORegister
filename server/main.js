import { Meteor } from 'meteor/meteor';
import LTOs from '../imports/api/ltos/collection.js';
import '../imports/startup/server/index.js'

Meteor.startup(() => {
  // code to run on server at startup
});
